{
  "scripts": {
    "validate": "tsc --noEmit",
    "build": "yarn parcel build src/index.html --public-url .",
    "serve": "yarn parcel serve src/index.html"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.1",
    "typescript": "^3.7.2"
  }
}
